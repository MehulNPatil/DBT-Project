# Name your project! Project names should contain only lowercase characters
# and underscores. A good package name should reflect your organization's
# name or the intended use of these models
name: "my_case_study_project"
version: "1.0.0"
config-version: 2

# This setting configures which "profile" dbt uses for this project.
profile: "snowflake" # This should match the profile name configured in DBT Cloud

# Model configurations
models:
  my_case_study_project:
    staging:
      stg_cust_mstr_kna1:
        +materialized: view
      stg_cust_mstr_tkna1:
        +materialized: view
      stg_invoice_raw:
        +materialized: view
      stg_prod_mstr_pna1:
        +materialized: view
      stg_prod_mstr_tpna1:
        +materialized: view
    marts:
      customer_product_summary:
        # schema: new_schema
        +materialized: table
      customer_region_summary:
        +materialized: table

# # Seed configurations
# seeds:
#   my_case_study_project:
#     country_code:
#       materialized: table
